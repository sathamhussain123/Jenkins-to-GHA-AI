version: "3.9"
services:
  converter:
    build: .
    volumes:
      - ./data/projects:/app/data/projects
    ports:
      - "8000:8000"
  ollama:
    image: ollama/ollama
    ports:
      - "11434:11434"